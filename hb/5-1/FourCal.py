class FourCal:
	def __init__(self):
		self.first =4
		self.second =2
	def setdata(self,first,second):
		self.first=first
		self.second=second
		return 
	def sum(self):
		result=self.first + self.second
		return result
	def min(self):
		result=self.first - self.second
		return result
	def mul(self):
		result = self.first * self.second
		return result
	def div(self):
		result =self.first / self.second
		return result
